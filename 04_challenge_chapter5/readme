1. untuk menjalankan, npm start / nodemon "nama file" (ket: Nodemon global location (port 8000))
2. list data login menggunakan http://localhost:8000/login/list
3. untuk login, menggunakan metode POST http://localhost:8000/login dengan variable id, nama dan password. 

